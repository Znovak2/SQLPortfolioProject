{
    "version": "2.0.0",

    "tasks": [
        {
            "label": "SQL Project: Build",
            "type": "process",
            "command": "dotnet",
            "args": [
                "build",
                "${workspaceFolder}/Bicycle Shop Analysis.sqlproj"
            ],
            "problemMatcher": "$sqlproj-problem-matcher",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "presentation": {
                "reveal": "always",
                "panel": "shared",
                "clear": true
            },
            "detail": "Builds the Bicycle Shop Analysis SQL project."
        },

        {
            "label": "Environment: Check prerequisites",
            "type": "shell",
            "command": "git --version; dotnet --version; sqlcmd -?",
            "problemMatcher": [],
            "presentation": {
                "reveal": "always",
                "panel": "shared",
                "clear": true
            },
            "detail": "Checks Git, .NET, and sqlcmd."
        },

        {
            "label": "SQL Server: Status",
            "type": "shell",

            "linux": {
                "command": "systemctl --no-pager --full status mssql-server"
            },

            "windows": {
                "command": "powershell -NoProfile -Command \"Get-Service -Name 'MSSQL*' | Format-Table -AutoSize\""
            },

            "command": "echo 'Native SQL Server service status is not available on this OS.'",

            "problemMatcher": [],
            "presentation": {
                "reveal": "always",
                "panel": "shared",
                "clear": true
            },

            "detail": "Shows the status of a locally installed SQL Server service."
        },

        {
            "label": "BikeStores: Validate database",
            "type": "process",
            "command": "sqlcmd",

            "args": [
                "-S",
                "${input:sqlServer}",
                "-U",
                "${input:sqlUser}",
                "-d",
                "BikeStores",
                "-b",
                "-Q",
                "SET NOCOUNT ON; SELECT DB_NAME() AS CurrentDatabase; SELECT COUNT(*) AS BaseTableCount FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_TYPE = 'BASE TABLE' AND TABLE_SCHEMA IN ('hr', 'production', 'sales'); SELECT COUNT(*) AS ProductCount FROM production.products; SELECT COUNT(*) AS OrderCount FROM sales.orders; SELECT COUNT(*) AS CandidateCount FROM hr.candidates; SELECT COUNT(*) AS EmployeeCount FROM hr.employees;"
            ],

            "options": {
                "env": {
                    "SQLCMDPASSWORD": "${input:sqlPassword}"
                }
            },

            "problemMatcher": [],

            "presentation": {
                "reveal": "always",
                "panel": "shared",
                "clear": true
            },

            "detail": "Validates the BikeStores and HR demonstration tables without changing data."
        },

        {
            "label": "Queries: Run portfolio queries",
            "type": "process",
            "command": "sqlcmd",

            "args": [
                "-S",
                "${input:sqlServer}",
                "-U",
                "${input:sqlUser}",
                "-d",
                "BikeStores",
                "-b",
                "-i",
                "${workspaceFolder}/database/queries/ALIAS.sql",
                "-i",
                "${workspaceFolder}/database/queries/AND.sql",
                "-i",
                "${workspaceFolder}/database/queries/BETWEEN.sql",
                "-i",
                "${workspaceFolder}/database/queries/IN_SUBQUERY.sql",
                "-i",
                "${workspaceFolder}/database/queries/JOIN.sql",
                "-i",
                "${workspaceFolder}/database/queries/LIKE.sql",
                "-i",
                "${workspaceFolder}/database/queries/NULL.sql",
                "-i",
                "${workspaceFolder}/database/queries/OFFSET_FETCH.sql",
                "-i",
                "${workspaceFolder}/database/queries/OR.sql",
                "-i",
                "${workspaceFolder}/database/queries/ORDER_BY.sql",
                "-i",
                "${workspaceFolder}/database/queries/SELECT.sql",
                "-i",
                "${workspaceFolder}/database/queries/SELECT_DISTINCT.sql",
                "-i",
                "${workspaceFolder}/database/queries/SELECT_TOP.sql",
                "-i",
                "${workspaceFolder}/database/queries/WHERE.sql"
            ],

            "options": {
                "env": {
                    "SQLCMDPASSWORD": "${input:sqlPassword}"
                }
            },

            "problemMatcher": [],

            "presentation": {
                "reveal": "always",
                "panel": "shared",
                "clear": true
            },

            "detail": "Runs all checked-in portfolio queries against BikeStores."
        }
    ],

    "inputs": [
        {
            "id": "sqlServer",
            "type": "promptString",
            "description": "SQL Server host or instance",
            "default": "localhost"
        },
        {
            "id": "sqlUser",
            "type": "promptString",
            "description": "SQL Server username",
            "default": "sa"
        },
        {
            "id": "sqlPassword",
            "type": "promptString",
            "description": "SQL Server password",
            "password": true
        }
    ]
}
